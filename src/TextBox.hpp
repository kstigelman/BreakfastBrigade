#pragma once
#include <iostream>
#include <SFML/Graphics.hpp>

class TextBox {
	private:
		sf::Image profile;
		sf::Font font;
		sf::Text text;

		sf::Vector2f size;
	public:
		TextBox (sf::Vector2f) {

		}

};